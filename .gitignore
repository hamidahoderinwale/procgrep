# Byte-compiled
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution
build/
dist/
*.egg-info/
*.egg
.eggs/
wheels/

# Virtual environments
.venv/
venv/
env/
.python-version

# Testing
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.tox/
.nox/

# Type checking
.mypy_cache/
.ruff_cache/
.dmypy.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project-local outputs
output/
notebooks/
*.ipynb_checkpoints/
case-studies/interface/

# precomputed spine store lives in HF dataset midah/procgrep-spines, not git
data/*.parquet

# Claude Code internal (agent worktrees, etc.)
.claude/

# secrets
.env
.env.local

# Kept local; not part of the upstream repo (README is the canonical doc)
CHANGELOG.md

# The plateau RCT is a separate project, kept local; not part of the procgrep repo.
plateau/

# The runner package lockfile is large (>500 KB); its env is recreated by `uv sync`.
runner/uv.lock
runner/runs/
data/student/

# The ecosystem-map build outputs regenerate from docs/build/*.py; keep only the scripts.
docs/build/index.html
docs/build/data/
